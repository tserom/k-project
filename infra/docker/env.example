# 复制到 k-project 根目录并重命名为 .env（与 docker compose 命令在同一目录执行时生效）
# 需要时取消注释：

# MYSQL_HOST_PORT=3306
# MYSQL_IMAGE=mysql:8.0
# MYSQL_IMAGE=docker.m.daocloud.io/library/mysql:8.0
# MYSQL_IMAGE=public.ecr.aws/docker/library/mysql:8.0
